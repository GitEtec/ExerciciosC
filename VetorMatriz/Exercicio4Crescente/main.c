#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	int vetor[5] ,j ,i,maior[5] ;
	for(i = 0; i < 5; i++){
		printf("Digite um valor: ");
		scanf("%d",&vetor[i]);
		if(vetor[i] > vetor[i]){
			maior[i] 
		}
	for(j = 1; j < 5; j++){
		if(vetor[j] > vetor[j]){
			maior[j] 
		}
		printf("%d",maior[i]);
		}	
	}
	
	return 0;
}
